apiVersion: v1
kind: Pod
metadata:
  name: inception
  labels:
    release: inception
spec:
  containers:
  - name: seed
    image: "{{ .Values.image }}"
    volumeMounts:
    - name: seed
      mountPath: /seed
  restartPolicy: Never
  volumes:
  - name: seed
    persistentVolumeClaim:
      claimName: seed
